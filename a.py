import math
c=50
h=30
value = []
items=[x for x in input("Please entet seq of vallues").split(',')'
for d in items:
    value.append(stsr(int(round(math.sqrt((2 * c*d)/h)))))

print( (','.join(value))
